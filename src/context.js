import { createContext } from 'react';
// keys are the name of person, whereas array assigned to keys are load
// five ele in array represents 5 days of week
export const dayLoadContext = createContext();
